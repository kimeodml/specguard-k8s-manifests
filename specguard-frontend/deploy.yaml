apiVersion: apps/v1
kind: Deployment
metadata:
  name: specguard-frontend-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: specguard-frontend
  template:
    metadata:
      labels:
        app: specguard-frontend
    spec:
      containers:
      - name: specguard-frontend
        image: kimeodml/specguard-frontend:d0fe3ce2b79a79d1ce4b47633a2d6a5970b771c9
        resources:
          requests:
            memory: "128Mi"
            cpu: "500m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
        ports:
        - containerPort: 80
        envFrom:
          - configMapRef:
              name: frontend-config          

